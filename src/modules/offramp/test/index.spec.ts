import { Test, TestingModule } from '@nestjs/testing';

describe('Offramp Module', () => {
  it('should pass empty test since module has been removed', () => {
    // This is an empty test that always passes
    // The offramp module has been removed, but we keep this stub to avoid
    // breaking imports in the modules/index.spec.ts file
    expect(true).toBeTruthy();
  });
}); 